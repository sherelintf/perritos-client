import { Grid } from '@mui/material'
import styled from '@emotion/styled'

export const BigContainer = styled(Grid)({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'flex-start',
  maxWidth: '1440px',
  margin: 'auto',
  padding: '10px',
})
